# このファイルは不要になりました。
# GitHub Pages は Settings > Pages > Deploy from a branch > main / (root) で設定してください。
# This workflow is intentionally disabled.
on:
  workflow_dispatch:
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deploy via branch setting in Pages Settings."
